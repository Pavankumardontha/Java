public class Main {

 public static void main(String[] args) {
  
  /* wrapper class =  provides a way to use primitive data types as reference data types. Reference data types contain useful methods that can be used with 
  collections (ex.ArrayList)
  */
  //primitive  //wrapper
  //---------  //-------
  // boolean    Boolean
  // char       Character
  // int        Integer
  // double     Double
  /* Note that in reference data types,the first letter is always Capital.String is an example of reference data type. */
  // autoboxing = the automatic conversion that the Java compiler makes between the primitive types and their corresponding object wrapper classes
  // unboxing = the reverse of autoboxing. Automatic conversion of wrapper class to primitive
  
  Boolean a = true;
  Character b = '@';
  Integer c = 123;
  Double d = 3.14;
  
  
  
 }
 
}
